# Copy to .env.local and set your key. .env.local is gitignored — never commit it.
# This is read ONLY by the serverless function (api/generate-recap.js), server-side.
# Do NOT prefix it with VITE_ — that would bundle it into the client.
ANTHROPIC_API_KEY=sk-ant-...
